// If we need to store multiple values then declaring separate variables for different
// values is never a good idea
// So to overcome this difficulty we store multiple values in the form of a single variable which
// will be an array
// Array is the THE most used datatype in the history of Web development after Objects

// array is declared just like any other variable but enclosed within square brackets

// In javascript, a particular array can contain any kind of datatype not like other languages
// example  let a = ["Post 1", {}, null, true, 5];

let a = ["Post 1", "Post 2", "Post 3", "Post 4", "Post 5"];

// acceessing values of array
// console.log(a[0]);
// console.log(a[1]);
// console.log(a[2]);
// console.log(a[3]);
// console.log(a[4]);

// getting length of array
// console.log(a.length);

// adding an element in array
// a.push("Post 6");
// a.push("Post 7");
// a.push("Post 8");

// removing an element
// a.pop(); // removes last element
// a.pop(); // removes last element
console.log(a);
